<h1>New block</h1>

<% form_for([@section,@page,@block], :html => { :class => "ajax" }) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>